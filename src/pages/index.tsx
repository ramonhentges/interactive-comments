import type { NextPage } from 'next';
import Head from 'next/head';
import { AddComment, Comment } from '../components';
import { useCommentsStore } from '../stores';

const Home: NextPage = () => {
  const { comments } = useCommentsStore();
  return (
    <div className="bg-lightBlue bg-opacity-10 w-screen min-h-screen flex flex-col items-center py-8 sm:py-10">
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className="flex flex-col px-5 sm:px-14 w-full">
        <section className="flex flex-col">
          {comments.map(comment => (
            <Comment key={comment.id} comment={comment} />
          ))}
          <AddComment type="new" className="mt-4" />
        </section>
      </main>

      <footer></footer>
    </div>
  );
};

export default Home;
